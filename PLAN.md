# pytest Coverage 자동 향상 파이프라인 — 실행 계획

대상: 사내 파이썬 repository
전제: 사내 LLM(gemma 계열, OpenAI 호환 엔드포인트), GPT 대비 낮은 지시이행·툴콜 성능
불변 조건: **business logic(테스트 외 모든 파일)은 단 한 글자도 바뀌지 않는다**

---

## 1. 결론 — 멀티 Agent는 이 문제에 맞지 않는다

앞서 제안한 3-Agent(총괄/분석/작성) 구조는 강의 실습용으로는 의미가 있지만,
실제 사내 도입에는 다음 이유로 권하지 않습니다.

| 문제 | 내용 |
|---|---|
| **툴콜 신뢰도** | Agent 구조는 LLM이 도구를 "정확한 JSON 인자로, 올바른 순서로" 호출한다는 전제 위에 있습니다. gemma 계열 소형 모델은 이 부분이 가장 약합니다. 인자 하나가 틀리면 루프 전체가 멈춥니다. |
| **위임 = 정보 손실** | Lead→Writer로 넘기는 지시문을 LLM이 씁니다. 약한 모델일수록 "미커버 라인 번호를 빠뜨리지 않고 전달"하는 데 실패합니다. 사람이 짠 코드로 넘기면 100% 정확합니다. |
| **비결정성** | 같은 repo를 두 번 돌리면 다른 경로로 갑니다. 사내 도구는 재현 가능해야 CI에 붙일 수 있습니다. |
| **디버깅 불가** | 실패했을 때 프롬프트 문제인지, 툴콜 문제인지, 위임 문제인지 분리가 안 됩니다. |
| **비용** | 커버리지 공백 계산, 우선순위 정렬, 파일 선택은 **전부 결정론적 계산**입니다. LLM에게 시킬 이유가 없습니다. |

### 대신 취할 구조: 좁은 LLM + 결정론적 오케스트레이터

```
파이썬 코드가 하는 일 (LLM 없음, 100% 재현 가능)
  clone → venv → 의존성 설치 → 커버리지 측정 → 공백 분석 → 타깃 선정 →
  프롬프트 조립 → 생성물 검증 → pytest 실행 → 수락/롤백 → 회계 → 리포트

LLM이 하는 일 (딱 하나)
  "이 함수 하나에 대한 pytest 테스트 코드를 써라"  →  코드 블록 반환
```

LLM을 **에이전트가 아니라 순수 함수**로 씁니다. 상태도, 도구도, 대화 이력도 주지 않습니다.
호출 1회 = 타깃 1개 = 독립 컨텍스트. 약한 모델에 줄 수 있는 가장 유리한 조건입니다.

> 이건 성능을 포기하는 선택이 아닙니다. 검증이 자동이고 값싸기 때문에(테스트는 돌려보면
> 맞는지 바로 압니다) **모델의 1회 정답률이 낮아도 시도 횟수로 보완할 수 있습니다.**
> 이 성질을 쓰는 게 3절의 best-of-N 입니다.

---

## 2. 파이프라인

### Phase 0 — 환경 구축 (LLM 없음)

```
1. git clone <repo> workspace/<name>          (--depth 1, 작업 브랜치 생성)
2. python -m venv .venv
3. pip install -r requirements.txt
   + pyproject.toml/setup.py 있으면 pip install -e .
   + pip install pytest pytest-cov pytest-timeout
4. 원본 무결성 기준선: 테스트 외 전체 파일 SHA-256 매니페스트 저장
```

**여기가 실제 운영에서 가장 많이 깨지는 지점입니다.** requirements.txt에 사내 인덱스,
시스템 라이브러리, 특정 파이썬 버전이 걸려 있으면 그냥 실패합니다.
→ **L2 환경 자가수리**가 담당합니다. 결정론적 레시피(파이썬 버전 교체, `--only-binary`,
사내 미러, `--no-deps`) 우선 → LLM 폴백 1회(allowlist된 pip 명령만) → 그래도 안 되면
**import 가능한 모듈만으로 부분 진행**. 중단하지 않습니다. 상세는 `docs/SELF_HEALING.md` L2.

### Phase 1 — 베이스라인 측정 (LLM 없음)

```
pytest --cov=<pkg> --cov-branch --cov-report=json:baseline.json -p no:randomly
```

- 기존 테스트가 이미 실패하면 **고치지 않습니다.** `known_failures` 목록에 기록하고
  `--deselect` 로 제외한 뒤 베이스라인을 잡습니다. 남의 테스트를 건드리는 순간
  신뢰를 잃습니다.
- 수집조차 안 되면(import error) 중단하고 보고합니다.

### Phase 2 — 타깃 선정 (LLM 없음, AST + coverage.json)

함수/메서드 단위로 쪼갭니다. **모듈 단위로 주면 약한 모델은 반드시 실패합니다.**

각 타깃에 대해 계산:

```
uncovered_lines   : coverage.json 의 missing_lines ∩ 함수 라인 범위
complexity        : 분기 수 (ast.If/For/While/Try/BoolOp)
io_deps           : open/requests/socket/subprocess/datetime.now/random/os.environ 사용
arg_complexity    : 인자 타입 힌트 유무, 기본값, *args/**kwargs
is_pure           : io_deps 없음 + self 미사용 또는 순수 계산

우선순위 = uncovered_lines / (1 + io_deps*3 + complexity*0.5)
```

**순수 함수부터 처리합니다.** 토큰당 커버리지 획득량이 I/O 의존 함수의 5~10배입니다.
I/O 의존 타깃은 큐 뒤로 미루고, 예산이 남을 때만 시도합니다.

### Phase 3 — 생성 (LLM, 타깃당 1~N회 독립 호출)

타깃 하나에 대해 프롬프트를 **코드가 조립**합니다.

```
[시스템]  너는 pytest 테스트를 작성한다. 코드 블록 하나만 출력한다. 설명하지 않는다.

[컨텍스트]
  - 대상 함수 전문 (소스 그대로, 앞뒤 5줄 포함)
  - 함수 시그니처와 타입 힌트
  - import 경로:  from src.billing import calc_fee      ← 코드가 계산해서 박아줌
  - 이 모듈이 쓰는 외부 의존 목록
  - 같은 repo의 기존 테스트 1개 (스타일 few-shot)     ← 있으면 정답률이 크게 오름
  - 아직 실행되지 않은 라인 번호와 그 라인의 소스

[출력 형식]
  ```python
  <여기에만 작성>
  ```
```

약한 모델용 프롬프트 원칙:
- **JSON을 요구하지 않습니다.** 코드 펜스만 파싱합니다(정규식 + AST 파싱으로 검증).
- **import 문을 모델이 추론하게 하지 않습니다.** 정확한 import 줄을 프롬프트에 넣고
  "이 import를 그대로 쓰라"고 지시합니다. 소형 모델 실패 1위가 import 경로 오류입니다.
- 한 번에 테스트 함수 2~4개만 요구합니다. 많이 시키면 품질이 무너집니다.

### Phase 4 — 검증 (LLM 없음, 3단 관문)

```
관문 1  정적 검사     ast.parse 성공 / test_* 함수 존재 / assert 존재 /
                     tests/ 외 경로 쓰기 시도 없음 / os.system·subprocess·socket 미사용
관문 2  격리 실행     pytest <새 파일만> --timeout=30 --cov-append -p no:cacheprovider
                     (네트워크 차단 conftest 주입, tmp_path 강제)
관문 3  무결성 확인   테스트 외 전체 파일 SHA-256 재검사 → 하나라도 다르면 즉시 롤백
```

관문 3이 핵심입니다. 생성된 테스트가 **실행 중에** 소스 파일을 덮어쓸 수 있습니다
(잘못 쓴 fixture, `open(module.__file__, "w")` 같은 사고). 정적 검사만으로는 못 막습니다.

### Phase 5 — 수락 / 롤백 (LLM 없음)

```
통과 AND 커버리지 증가 > 0   → 수락. .coverage 누적, git add tests/
통과 BUT 커버리지 증가 = 0   → 폐기 (중복 테스트)
실패                        → Phase 6 수리로
무결성 위반                 → 즉시 폐기 + 해당 타깃 영구 블랙리스트 + 경보
```

**수락된 것만 남기고 나머지는 흔적 없이 버립니다.** 이 greedy accept/rollback 구조 덕분에
모델이 10번 중 3번만 성공해도 파이프라인 전체는 단조 증가합니다.
약한 모델을 쓰는 핵심 안전장치입니다.

### Phase 6 — 수리 (LLM, 최대 2회)

실패 로그를 **압축해서** 다시 넣습니다. 전체 트레이스백을 주면 소형 모델은 길이에 압도됩니다.

```
주는 것: 실패한 테스트 이름 / 예외 타입 / 예외 메시지 한 줄 /
        실패 지점 소스 3줄 / (AssertionError면) 실제값과 기대값
주지 않는 것: 전체 트레이스백, 다른 테스트 결과, 이전 대화 이력
```

2회 실패하면 **버리고 다음 타깃으로 갑니다.** 소형 모델은 같은 오류를 반복하는 경향이
강해서, 3회 이상 재시도는 거의 항상 토큰 낭비입니다.

### Phase 7 — 게이트 판정

**80%는 사내 하드 게이트입니다.** 정체를 종료 조건으로 쓰지 않고 **승급 조건**으로 씁니다.

| 조건 | 처리 |
|---|---|
| 전체 커버리지 ≥ 80% **AND 전체 스위트 완주 통과** | 성공 종료 |
| 10타깃 연속 수락 0건 | **L3 에스컬레이션 사다리 S1→S2→S3→S4** (중단 아님) |
| 사다리 소진 & 80% 미달 | **L4 리팩터 승인 게이트** (사람 승인 → 적용 → 자동 검증 → 롤백) |
| 승인 거절 or 그래도 미달 | exit 1 + 라인 단위 귀속 리포트. 통과한 척하지 않음 |
| LLM 호출/시간 예산 초과 | 중단, 현재까지 결과 확정 + 도달 단계 기록 |

판정은 **`pytest --cov --cov-fail-under=80` 종료 코드 0** 으로만 합니다.
누적 `.coverage` 숫자로 선언하지 않습니다 — 개별 격리 실행에서 안 나던 테스트 간 간섭이
전체 완주에서 드러나기 때문입니다(**L5 스위트 자가수리**).

통과 보고 시 **정직성 세트 4종**(characterize 비율 / 분모 제외 전·후 커버리지 /
mutation score / 도달한 에스컬레이션 단계)을 항상 함께 냅니다.

---

## 3. 약한 모델(gemma)을 전제로 한 핵심 전략

### 3-1. best-of-N — 가장 큰 레버

검증이 자동이고 값싸므로, 타깃 하나에 **temperature 0.8로 N개(권장 3~5개) 후보를 생성**하고
전부 검증한 뒤 **커버리지를 가장 많이 올린 것 하나만 채택**합니다.

정답률 30% 모델도 4회 시도하면 최소 1개 성공 확률이 76%로 올라갑니다.
소형 모델이므로 추론 비용이 싸다는 점이 그대로 이점이 됩니다. GPT로 1번 잘 뽑는 것보다
gemma로 5번 뽑아 자동 채점하는 쪽이 이 문제에서는 더 쌉니다.

### 3-2. Characterization 모드 — 권장 기본값

소형 모델이 가장 많이 틀리는 부분은 테스트 구조가 아니라 **기대값 예측**입니다.
`assert calc_fee(1000, "VIP") == 850` 에서 850을 맞추려면 로직을 정확히 추론해야 합니다.
여기서 AssertionError가 대량 발생합니다.

그래서 역할을 나눕니다.

```
LLM이 쓰는 것   : 호출부 — 어떤 입력으로 부를 것인가 (다양한 경계값, 예외 경로)
하네스가 쓰는 것 : 단언부 — 실제로 실행해 반환값을 캡처하고 assert 를 코드가 생성
```

생성 결과:

```python
def test_characterize_calc_fee_vip():
    result = calc_fee(1000, "VIP")
    assert result == 850          # ← 실행 결과로부터 자동 생성됨
```

**장점**: 소형 모델 성공률이 극적으로 올라갑니다. 회귀 방지 효과는 동일합니다.
**단점이자 반드시 알아야 할 점**: 현재 동작을 정답으로 고정하므로, **버그가 있으면 버그를
정답으로 박아버립니다.** 레거시 코드에 커버리지를 씌우는 표준 기법(characterization test)이지만
공짜가 아닙니다.

완화책:
- 파일명·함수명에 `test_characterize_` 접두사를 강제해 일반 테스트와 구분합니다.
- 캡처된 기대값이 `None`, 빈 컬렉션, 예외인 경우는 **의심 목록**에 올려 리포트에 별도 표기합니다.
- PR 설명에 "이 테스트들은 현재 동작을 기록한 것이며 정답성은 검토가 필요함"을 명시합니다.

**엄격 모드**(모델이 기대값까지 작성)도 옵션으로 두되, gemma에서는 성공률이 크게 떨어질 것으로
보고 두 모드의 실측 수치를 비교해 결정하는 것을 권합니다.

### 3-3. 그 외

- **few-shot은 같은 repo에서 뽑습니다.** 기존 테스트 파일 1개를 프롬프트에 넣으면
  import 스타일·fixture 관례를 따라 합니다. 범용 예시보다 훨씬 효과적입니다.
- **컨텍스트를 짧게 유지합니다.** gemma 계열은 긴 컨텍스트에서 중간 정보를 놓칩니다.
  프롬프트 목표 상한 4K 토큰.
- **온도 분리**: 1차 시도 0.2(안정), best-of-N 추가 후보 0.8(다양성), 수리 0.3.

---

## 4. business logic 불변 보장 (3중 방어)

| 층 | 수단 | 막는 것 |
|---|---|---|
| 1. 쓰기 경로 | 파일 쓰기 함수가 `tests/` 하위 절대경로만 허용. 상위 탈출(`..`) 정규화 후 차단 | 생성 단계의 잘못된 경로 |
| 2. 정적 검사 | `open(...,'w')`, `pathlib.write_*`, `shutil`, `subprocess`, `os.remove` 사용 시 거부 | 실행 시 파일 조작 시도 |
| 3. 무결성 해시 | 매 pytest 실행 **직후** 테스트 외 전체 파일 SHA-256 재검증 | 1·2를 우회한 모든 변경 |
| 보험 | git 작업 브랜치 + 매 배치 후 `git diff --quiet -- ':!tests'` 강제 | 최종 안전망 |

위반 시: 해당 테스트 폐기 → 워크스페이스를 원본 커밋으로 `git checkout -- .` → 경보 기록.
관문 3이 걸리면 **파이프라인을 계속 돌리지 않고 멈추는 것**을 기본값으로 둡니다.

---

## 5. 커버리지 회계

타깃마다 전체 스위트를 다시 도는 건 대형 repo에서 감당이 안 됩니다.

```
누적 .coverage 파일 하나를 유지
  - 베이스라인 측정 시 생성
  - 후보 테스트는 --cov-append 로 격리 실행 → 이 후보의 순수 기여분 계산
  - 수락되면 누적본에 반영, 폐기되면 누적본 롤백(수락 전 스냅샷 복원)
  - 10배치마다 1회 전체 재측정으로 드리프트 보정
```

기여분이 0인 후보를 버리는 판단이 여기서 나옵니다. 중복 테스트가 쌓이면 스위트 실행 시간만
늘고 커버리지는 안 오릅니다.

---

## 6. 구조와 설정

```
self-healing/
├── .env / .env.example
├── requirements.txt
├── main.py                  # CLI: python main.py --repo <url|path> --target 80
├── core/
│   ├── env_setup.py         # clone, venv, pip install
│   ├── coverage_io.py       # 측정, 파싱, 누적 회계
│   ├── targets.py           # AST 분석, 함수 단위 분해, 우선순위
│   ├── prompt.py            # 프롬프트 조립 (few-shot, import 주입)
│   ├── llm.py               # OpenAI 호환 클라이언트 (사내 엔드포인트)
│   ├── validate.py          # 3단 관문
│   ├── integrity.py         # SHA-256 매니페스트
│   ├── characterize.py      # 실행 캡처 → assert 자동 생성
│   └── loop.py              # 오케스트레이션, 수락/롤백, 예산
├── workspace/               # clone 대상 (git-ignored)
└── reports/
    ├── run_<ts>.md          # 전/후 커버리지, 수락/폐기 통계, 의심 목록
    └── metrics.json
```

`.env` 는 OpenAI 호환 규격으로 잡았습니다. 사내 gemma가 vLLM/Ollama/TGI 어느 것으로
서빙되든 `LLM_BASE_URL` 만 바꾸면 됩니다. 실제 엔드포인트 주소와 모델 ID는 확인해서
채워 주세요.

---

## 7. 80% 게이트를 어떻게 실제로 넘기는가

목표는 **80% 하드 게이트 통과**입니다(사내 규칙). 넘기는 경로는 하나가 아니라 사다리입니다.
상세는 `docs/SELF_HEALING.md`.

```
S0 순수 함수            ──┐
S1 후보 증폭 (best-of-6)  │  여기까지로 대부분의 repo가 80%를 넘습니다
S2 타깃 재분해 (라인 단위)─┘  ← 사다리의 주력
S3 I/O 의존 대상 개방
S4 분모 정리 (사람 승인)
L4 리팩터 승인 게이트      ← 하드 천장을 뚫는 유일한 정직한 경로
```

**S2가 핵심입니다.** 소형 모델은 "이 함수의 테스트를 써라"는 넓은 지시에 약하지만
"127~131번 라인을 지나게 하는 입력을 만들어라"는 좁은 지시에는 훨씬 강합니다.
프롬프트를 다듬는 것보다 **타깃을 잘게 쪼개는 쪽이 효과가 큽니다.**

**L4까지 가면 사람 승인이 필요합니다.** 자동으로 business code를 고치지 않습니다.
대신 승인 후 적용 결과는 characterization test가 "동작 불변"을 자동 증명하고,
실패하면 git 자동 롤백됩니다. 리팩터링조차 self-healing 루프 안에 있습니다.

### 실측이 필요한 수치

gemma 계열 1회 시도 성공률은 순수 함수 + characterize 모드에서 50~70%,
strict 모드에서 20~40%로 예상합니다. best-of-N과 L0(재호출 없는 결정론적 교정)으로 보완합니다.
**구현 3단계에서 이 수치를 실측하는 것이 최우선입니다.**
20% 미만이 나오면 프롬프트가 아니라 S2(타깃 분해 단위)를 먼저 당겨야 한다는 신호입니다.

### 게이트를 못 넘겼을 때

**통과한 척하지 않습니다.** exit 1 과 함께 남은 공백을
`순수로직 / IO의존 / 프레임워크 / 데드코드 / 리팩터필요` 로 **라인 단위 귀속**해 보고합니다.
80%를 못 넘긴 repo는 "실패"가 아니라 "이 5가지 중 무엇이 몇 줄이라서 못 넘겼다"가
나와야 다음 행동이 정해집니다.

---

## 8. 구현 순서

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | Phase 0~2 (환경 구축 + 베이스라인 + 타깃 분해). **LLM 없음** | 임의 repo에서 "미커버 함수 우선순위 목록"이 출력됨 |
| 2 | `llm.py` + 단일 타깃 1회 생성/검증 | 순수 함수 1개에 대해 통과하는 테스트가 나옴 |
| 3 | **성공률 실측** — 순수 함수 30개에 각 모드로 1회씩 | characterization vs 엄격 모드 성공률 표 |
| 4 | 루프 + 수락/롤백 + 무결성 3중 방어 | 커버리지 단조 증가, `git diff` 항상 tests/ 만 |
| 5 | best-of-N, 예산 관리, 리포트 | 전체 실행 후 report 자동 생성 |
| 6 | (선택) CI 연동 — 야간 실행, PR 자동 생성 | — |

3단계에서 나온 수치가 나머지 전부의 설계 근거가 됩니다. 여기를 건너뛰고 루프부터 만들면
나중에 "왜 커버리지가 안 오르는지" 원인 분리가 안 됩니다.
